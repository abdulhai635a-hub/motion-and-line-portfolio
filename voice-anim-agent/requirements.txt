# Stage 1 (forced alignment) only. Everything else in this folder is stdlib Python 3.9+.
# Install into a virtualenv:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# WhisperX pulls in torch; on CPU-only machines install the CPU torch wheel first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
whisperx>=3.1.1

# Optional fallback aligner for languages WhisperX has no wav2vec2 model for (PRD 10).
# aeneas needs system packages (espeak, ffmpeg); install it only if you actually need it.
# aeneas>=1.7.3
